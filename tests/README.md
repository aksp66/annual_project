# tests/

Tests unitaires : pipeline de données (`test_data.py`), processus de diffusion forward (`test_diffusion.py`), U-Net et processus inverse (`test_unet.py`), DCGAN (`test_gan.py`), métriques d'évaluation FID/diversité (`test_metrics.py`), API FastAPI (`test_api.py`), reproductibilité seed+config (`test_reproducibility.py`).
