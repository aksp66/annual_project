# docker/

Conteneurisation du projet (Dockerfiles pour `app/api` et `app/web`, `docker-compose.yml` à la racine pour orchestrer les services).

À remplir une fois l'API et l'application développées — cf. `HISTORY.md` pour le suivi.
